{% extends '::layout.html.twig' %}

{% block title %}
    {{ "oro.issue.new_issue_header"|trans }} -
    {{ parent() }}
{% endblock %}


{% block content %}
    <h1>{{ "oro.issue.new_issue_header"|trans }}</h1>

    <form action="{{ path('issue_create', {'project': entity.project.id}) }}" method="post" {{ form_enctype(form) }} role="form" class="form">
        {{ form_errors(form) }}

        <div class="form-group">
            {{ form_row(form.summary) }}
        </div>

        <div class="form-group">
            {{ form_row(form.description) }}
        </div>

        <hr/>

        <div class="form-group">
            {{ form_row(form.issueType) }}
        </div>

        <div class="form-group hidden" id="parent-block">
            {{ form_row(form.parent) }}
        </div>

        <hr/>

        <div class="form-group">
            {{ form_row(form.issuePriority) }}
        </div>
        <div class="form-group">
            {{ form_row(form.issueStatus) }}
        </div>
        <div class="form-group">
            {{ form_row(form.issueResolution) }}
        </div>

        <hr/>

        <div class="form-group">
            {{ form_row(form.assignee) }}
        </div>

        <div>
            <button type="submit" class="btn btn-primary">{{ "oro.issue.create_issue_button"|trans }}</button>
        </div>

        {{ form_rest(form) }}
    </form>

    {% include 'OroIssueBundle:Issue:parentField.html.twig' %}
{% endblock %}
